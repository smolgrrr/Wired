export const DEFAULT_DIFFICULTY = 16;

export const DEFAULT_RELAYS = [
  "wss://powrelay.xyz",
  "wss://pow.relays.land",
  "wss://relay.damus.io",
  "wss://offchain.pub",
  "wss://nos.lol",
] as const;