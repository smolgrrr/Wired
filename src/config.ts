export const DEFAULT_DIFFICULTY = 16;

export const DEFAULT_RELAYS = [
  "wss://powrelay.xyz",
  "wss://pow.relays.land",
] as const;

export const QUOTE_FALLBACK_RELAYS = [
  "wss://relay.damus.io",
  "wss://offchain.pub",
  "wss://nos.lol",
] as const;